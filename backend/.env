# Copy to .env and set values
APP_SECRET=monte2021
SQLITE_PATH=app.db

# Frontend origins for CORS (comma or semicolon separated, or JSON list)
# For the Node-free UI (served by FastAPI on the same origin), this is not required.
# Example single origin: http://localhost:5173
FRONTEND_ORIGINS=[]

# Default admin bootstrap (used on first run if no users exist)
BOOTSTRAP_ADMIN_EMAIL=josh@manifold.digital
BOOTSTRAP_ADMIN_PASSWORD=monte2021

# Jira connection defaults (can be set in the Admin UI)
JIRA_BASE_URL=https://montefiore-einstein.atlassian.net/
JIRA_EMAIL=josh@montefioreeinstein.org
JIRA_API_TOKEN=

DEFAULT_WINDOW_DAYS=180
BUSINESS_HOURS_START=09:00
BUSINESS_HOURS_END=17:00
BUSINESS_DAYS=Mon,Tue,Wed,Thu,Fri
TIMEZONE=America/New_York
